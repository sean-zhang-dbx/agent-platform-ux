bundle:
  name: agent-platform-ux

resources:
  apps:
    app:
      name: "agent-platform-ux"
      description: "Agent platform UX prototype (synthetic data)"
      source_code_path: ./

      # Start the app automatically on deploy.
      lifecycle:
        started: true
      # Uncomment to enable on behalf of user API scopes. Available scopes: sql, dashboards.genie, files.files, serving.serving-endpoints
      # user_api_scopes:
      #   - sql

targets:
  default:
    default: true
    workspace:
      host: https://your-workspace.cloud.databricks.com
